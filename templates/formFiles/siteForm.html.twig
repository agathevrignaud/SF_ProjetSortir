{% extends 'base.html.twig' %}
{% block title %}Ajouter un site | Sortir.com{% endblock %}
{% block body %}
    {{ include('utils/header.html.twig') }}

    <div class="container">
        <h3 class="text-center py-3">Ajouter un nouveau site</h3>
        <div class="tableFixHeadCity">
            {{ form_start(siteForm) }}
            <div class="form-group pb-2 text-center">
                <label for="nom" class="col-lg-2">{{ form_label(siteForm.nom) }} * :</label>
                {{ form_widget(siteForm.nom) }}
            </div>

        </div>
        <div class="form-group text-center">
            <button type="submit" class="btn btn-primary btn-lg p-3">Créer un nouveau site</button>
            {{ form_end(siteForm) }}
        </div>

    </div>

    {{ include('utils/footer.html.twig') }}
{% endblock %}
